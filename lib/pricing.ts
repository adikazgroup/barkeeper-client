/**
 * The commercial numbers the marketing copy quotes.
 *
 * They appear on the pricing page, in the sign-up shell and in the closing
 * calls to action, so they live here rather than being retyped — a trial
 * length that disagrees between two pages is a promise the product breaks.
 */

/** Days free on every plan, no card taken. */
export const TRIAL_DAYS = 14;

/** Currency the plan prices below are quoted in. */
export const CURRENCY = "USD";
